---
 - hosts: localhost
   connection: local
   gather_facts: false
   user: root
   pre_tasks:
    - include_vars: ~/Documents/AnsibleLab/roles/provision-ec2-instance/ec2-vars/{{type}}.yml
   roles:
    - provision-ec2-instance
    - nginx
